// Load testing skeleton simulating peak traffic on claim routes
export function simulateBurstTraffic(requestsPerSecond: number) {
  console.log(`Simulating ${requestsPerSecond} req/sec on /claim/...`);
}
